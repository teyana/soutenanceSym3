{% extends 'base.html.twig' %}

{% block title %}Edit Product
{% endblock %}

{% block body %}
	<div class="container border shadow col-8">
		<h1 class="my-5 text-center">Modifier:
			{{product.name}}
		</h1>
		<div class="container">

			{{ include('admin/product/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

			<a class="btn btn-warning my-2 mx-2" href="{{ path('product_index') }}">Retour</a>

			{{ include('admin/product/_delete_form.html.twig') }}
		</div>
	</div>
{% endblock %}
